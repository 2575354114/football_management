# 数据库配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/football_management?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Liangzhi020918

# MyBatis配置
mybatis.type-aliases-package=com.lz.football_management.entity
mybatis.mapper-locations=classpath:mapper/*.xml

# Thymeleaf配置
spring.thymeleaf.cache=false

# Servlet配置
spring.servlet.multipart.max-file-size=30MB
spring.servlet.multipart.max-request-size=30MB

# 静态资源配置
spring.mvc.static-path-pattern=/static/**
spring.web.resources.static-locations=classpath:/static/

# 日志配置
logging.level.com.lz.football_management.dao=debug

# 服务器端口配置
server.port=4444
